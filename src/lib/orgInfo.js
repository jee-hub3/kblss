/**
 * 조직 사실(연락처·위치·운영진·정기 회의)의 단일 소스.
 *
 * 같은 사실이 여러 페이지에 흩어져 있으면 동기화는 반드시 실패한다 —
 * 실제로 같은 규정 문구가 FAQ와 /apply에서 어긋난 채 발견된 적이 있다.
 * 여기 있는 값을 고치면 참조하는 모든 화면이 함께 바뀐다.
 *
 * 표현(설명문·아이콘·문장 구성)은 각 페이지에 남기고, 여기에는
 * "사실"만 둔다. 예: 이름은 '이상곤'이고 "이상곤 교수님" 같은
 * 표기는 쓰는 자리에서 조합한다.
 *
 * 특히 email은 개인정보처리방침의 문의처와 같아야 한다 —
 * 어긋나면 열람·정정 요구가 도달하지 않는 의무 이행 문제가 된다.
 */
export const ORG_INFO = {
    /** 대표 이메일 — 방침·푸터·FAQ·지원 안내가 모두 이 값을 쓴다 */
    email: 'keybridgeleaders@gmail.com',

    /* 인스타그램 항목은 두지 않는다 — 계정 URL이 없어서가 아니라
       2026-08 운영진이 SNS 채널 자체를 폐기하기로 결정했다.
       새 채널을 열기로 결정하기 전에는 복원하지 말 것. */

    location: {
        campus: '한국기술교육대학교 제1캠퍼스',
        room: '인문경영관 212호',
    },

    /**
     * 정기 회의 — ★ 매 학기 확인 대상.
     *
     * 2026-08-27 오너 확정: 매주 수요일 19시. (그 전에는 '주 1회'만 확정되고
     * 요일이 미정이라 day/time을 비우고 note로 미정임을 알렸다.)
     *
     * ★ 다시 미정 상태가 되면 day/time을 null로 되돌리고 note를 채우면 된다 —
     *   getMeetingLabel()이 "주 1회"로 돌아가고 미정 안내가 다시 붙는다.
     *   호출부(FAQ 답변, /apply 활동 안내·Rules & Duties, 지원서 동의 문구)는
     *   note가 비어 있으면 알아서 생략하므로 손댈 필요 없다.
     */
    meeting: {
        frequency: '주 1회',
        day: '수요일',
        time: '19시',
        note: '',
    },

    professor: { role: '지도교수', name: '이상곤' },

    /**
     * 지원 자격 — /apply 제목과 FAQ 답변이 함께 읽는다.
     * ★ 두 축을 한 문장에 담아야 한다: 신분(학부 재학·휴학생)과 전공·학년(제한 없음).
     *   한쪽 축만 적으면 다른 화면과 어긋난다 — 2026-08 실제로 그렇게 어긋났다.
     *   FAQ는 전공·학년 축만 말해 "누구나"로 읽혔고, /apply는 신분 축만 말했다.
     *   두 문구가 모순처럼 보여 신분 제한 쪽이 지워진 채 배포된 적이 있다.
     * 대학원생·졸업생은 대상이 아니다(오너 확정 2026-08-25).
     * 폼의 학년 선택지(Recruit.jsx)도 이 자격과 맞춰야 한다 — '휴학중'까지가 전부다.
     */
    eligibility: {
        short: '학부 재학생·휴학생 대상',
        full: '학부 재학생·휴학생이라면 전공과 학년에 관계없이 지원할 수 있습니다.',
    },

    /* 경고 규정(warning)은 2026-08-27 오너 지시로 제거했다 —
       /apply의 'Rules & Duties' 아코디언 항목과 FAQ 질문 한 개가 이 값을 읽었고
       둘 다 함께 삭제했다. 다시 고지하기로 결정하기 전에는 복원하지 말 것. */

    /**
     * 운영진 — 2026-08 개편으로 5인 → 6인. 배열 순서가 곧 화면 노출 순서다.
     *
     * ★ 읽는 쪽은 순서·개수에 기대지 말 것. 앞에서 N명만 구조분해로 꺼내면
     *   인원이 늘 때 뒤쪽 사람이 에러 없이 조용히 사라진다 — 이번 개편에서
     *   실제로 6번째가 화면에서 증발할 뻔했다(Organization.jsx). 필요한 사람은
     *   role로 찾거나 tier로 거를 것. 순서를 바꿔도 짝이 어긋나지 않는다.
     *
     * tier — 조직도상의 층이자 사실. 'lead'는 기능이 아니라 운영 전체를
     *   총괄하는 자리(랩실장·부랩실장)이고, 'officer'는 기능별 임원이다.
     *   조직 페이지의 레벨 구분과 /apply 문의처가 이 값을 읽는다.
     *
     * 직함 체계는 2026-08 개편에서 '담당' → '임원'으로 바뀌었다.
     * 단, '랩실장·부랩실장'은 ADR-11에서 확정한 표기라 그대로 둔다.
     */
    leads: [
        { role: '랩실장', name: '조용운', tier: 'lead' },
        { role: '부랩실장', name: '박상현', tier: 'lead' },
        { role: '일정·기획 임원', name: '김나경', tier: 'officer' },
        { role: '프로젝트 임원', name: '김태우', tier: 'officer' },
        { role: '공모전 임원', name: '노담희', tier: 'officer' },
        { role: '스터디 임원', name: '박태준', tier: 'officer' },
    ],
};

/**
 * tier로 운영진을 거른다 — 배열 순서(=노출 순서)는 그대로 유지된다.
 * 인원이 늘거나 순서가 바뀌어도 호출부를 고칠 필요가 없다.
 */
export const getLeadsByTier = (tier) => ORG_INFO.leads.filter((lead) => lead.tier === tier);

/**
 * 정기 회의 주기 문구 — 요일이 정해지기 전에는 빈도만 말한다.
 *
 * 호출부가 각자 문장을 조립하면 요일 확정 시점에 일부만 갱신되어 어긋난다
 * (경고 규정이 FAQ·/apply에서 갈렸던 것과 같은 실패). 문구는 여기서만 만든다.
 *
 *   요일 미정: '주 1회'
 *   요일 확정: '매주 수요일 19시'
 */
export const getMeetingLabel = () => {
    const { frequency, day, time } = ORG_INFO.meeting;
    if (!day) return frequency;
    return `매주 ${day}${time ? ` ${time}` : ''}`;
};
